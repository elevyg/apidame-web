export const notasIndexTitleName = "notas-index-title";

export function notaCoverName(slug: string) {
  return `nota-cover-${slug}`;
}

export function notaTitleName(slug: string) {
  return `nota-title-${slug}`;
}
